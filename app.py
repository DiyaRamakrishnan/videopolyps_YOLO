import streamlit as st
import numpy as np
import cv2
from tensorflow.keras.models import load_model
from ultralytics import YOLO

def main():
    st.title('Streamlit App with Imports')
    st.write('Hello, World!')

if __name__ == "__main__":
    app.run(debug=False)